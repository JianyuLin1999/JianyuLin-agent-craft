# write-for-reader 更名为 remove-ai-tone

- Skill 目录和 `name` 改为 `remove-ai-tone`。
- README、相关 Skill 和 HTML 中的引用同步使用新名称。
- HTML 说明改为 `docs/remove-ai-tone-guide.html`。
- 0.8.0 明确排除虚构作品，并删除 `profiles/creative.md`。
- 主文件增加“自然、克制”和“不要用笼统评价代替具体内容”两项通用要求。
- 新增精简的 `profiles/explanation.md`，接替 `deep-explainer` 和 `bestie-explainer` 中仍有价值的解释与排错要求。
- 0.8.1 要求解释帮助读者建立可复述、可用于判断新情况的理解，并补充动机、因果关系和具体例子。
